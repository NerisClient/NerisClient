document.addEventListener("DOMContentLoaded", () => {
    const freeButton = document.querySelector(".free-button");

    freeButton.addEventListener("click", () => {
        alert("Ссылка на бесплатную версию будет добавлена позже!");
    });
});
